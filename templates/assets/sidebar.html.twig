<ul class="navbar-nav bg-gradient-primary sidebar sidebar-dark accordion" id="accordionSidebar">

            <!-- Sidebar - Brand -->
            <a class="sidebar-brand d-flex align-items-center justify-content-center" href="index.html">
                {# <div class="sidebar-brand-icon rotate-n-15">
                    <i class="fas fa-laugh-wink"></i>
                </div> #}
                <div class="sidebar-brand-text mx-3">
                    <img src="img/logo.png" width='200px' height='110px' />
                </div>
            </a>

            <!-- Divider -->
            <hr class="sidebar-divider my-0">

            <!-- Nav Item - Dashboard -->
            <li class="nav-item">
                <a class="nav-link" href=" {{ path('usuarioTramiteIndividual')}} ">
                    <i class="fas fa-fw fa-tachometer-alt"></i>
                    <span>Tramites asignados a mi</span></a>
            </li>

            <!-- Divider -->
            <hr class="sidebar-divider">

            <!-- Heading -->
            <div class="sidebar-heading">
                Panel
            </div>

            <!-- Nav Item - Pages Collapse Menu -->
            {% if is_granted('ROLE_ADMIN') %}
            <li class="nav-item">
                <a class="nav-link collapsed" href="{{ path('app_register')}}" >
                    <i class="fas fa-fw fa-users"></i>
                    <span>Usuarios</span>
                </a>
            </li>
             {% endif %}

            <li class="nav-item">
                <a class="nav-link collapsed" href="{{ path('cliente')}} " >
                    <i class="fas fa-fw fa-user"></i>
                    <span>Clientes</span>
                </a>
            </li>

             <!-- Nav Item - Trámites Menu -->
             
            <li class="nav-item">
                <a class="nav-link collapsed" href="#" data-toggle="collapse" data-target="#collapseTramites"
                    aria-expanded="true" aria-controls="collapseTramites">
                    <i class="fas fa-fw fa-gavel"></i>
                    <span>Tramites</span>
                </a>
                <div id="collapseTramites" class="collapse" aria-labelledby="headingUtilities"
                    data-parent="#accordionSidebar">
                    <div class="bg-white py-2 collapse-inner rounded">
                        <h6 class="collapse-header">Custom Utilities:</h6>

                        <a class="collapse-item" href=" {{ path('tipoTransferencia')}} ">Tipo de Tramites</a>
                        <a class="collapse-item" href= " {{ path('clienteTramite')}} " >Tramites</a>
                        {% if is_granted('ROLE_ADMIN') %}
                                  
                        <a class="collapse-item" href= " {{ path('usuarioTramite')}} " >Todos losTramites</a>
                         {% endif %}          
                    </div>
                </div>
            </li>

             <!-- Nav Item - Reportes Menu -->
              
               {% if is_granted('ROLE_ADMIN') %}
            <li class="nav-item">
                <a class="nav-link collapsed" href="#" data-toggle="collapse" data-target="#collapseRoles"
                    aria-expanded="true" aria-controls="collapseRoles">
                    <i class="fas fa-fw fa-file"></i>
                    <span>Reportes</span>
                </a>
                <div id="collapseRoles" class="collapse" aria-labelledby="headingUtilities"
                    data-parent="#accordionSidebar">
                    <div class="bg-white py-2 collapse-inner rounded">
                        <h6 class="collapse-header">Custom Utilities:</h6>
                        <a class="collapse-item" href="{{ path('download')}}">Reporte trámites</a>
                        <a class="collapse-item" href="{{ path('reporte')}}">Reporte trámites por tipo</a>
                    </div>
                </div>
            </li>
            {% endif %}
            <!-- Divider -->
            <hr class="sidebar-divider">

            <!-- Heading -->
            
            

            

            <!-- Sidebar Toggler (Sidebar) -->
            <div class="text-center d-none d-md-inline">
                <button class="rounded-circle border-0" id="sidebarToggle"></button>
            </div>

        </ul>